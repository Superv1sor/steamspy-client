import React from "react";
import logo from "../../img/steamis_logo.gif";

const Header = () => {
  return (
    <header>
      <img src={logo} alt="STEAM.is" />
    </header>
  );
};

export default Header;
